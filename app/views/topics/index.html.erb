<h1>Themen</h1>
<% @own_topics.each do |topic| %>
<div class="col-md-4">
	<div class="topic">
		<h2 class="topic-title"><%= link_to topic.title, topic_path(topic) %></h2>
	</div>
</div>
<% end %>
<h1>Themen</h1>
<% @topics.each do |topic| %>
<!--% if current_user == topic.user || current_user.admin? || topic.users.include?(current_user) %-->
<div class="col-md-4">
	<div class="topic">
		<h2 class="topic-title"><%= link_to topic.title, topic_path(topic) %></h2>
		<p class="topic-author"><%= topic.user.email %></p>
	</div>
</div>
<!--% end %-->
<% end %>